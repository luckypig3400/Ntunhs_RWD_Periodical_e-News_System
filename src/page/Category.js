import React from "react";
const config = require('../config/default.json')
const apiURL = config.apiURL;

function Category() {
  return (
    <>
      <div className="headerTitle">分類管理</div>
    </>
  );
}

export default Category;
